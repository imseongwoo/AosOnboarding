pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "AosOnboarding"
include ':app'
include ':core'
include ':features'
include ':features:feature-login'
include ':features:feature-signup'
include ':ui-components'
